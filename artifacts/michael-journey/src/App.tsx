/**
 * App.tsx
 * -------
 * Root of the Michael Journey React app.
 * Renders the Phaser game canvas directly — no navigation needed.
 */

import Game from "@/pages/Game";

export default function App() {
  return <Game />;
}
